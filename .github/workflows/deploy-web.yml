name: Deploy web app to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: deploy-web
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.6"
          channel: "stable"

      - run: flutter pub get

      # --no-web-resources-cdn bundles CanvasKit locally instead of loading it
      # from gstatic.com, so the app loads on networks where that CDN is
      # blocked (matches build.yml's build-web job).
      - run: flutter build web --release --base-href /maridive-fleet-vessels/ --no-web-resources-cdn

      - uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: build/web
          publish_branch: gh-pages
